# G2G ephemeral goal file (armed by /g2g:build, deleted at terminal states)
.g2g-goal
# G2G goal liveness sidecar (heartbeat timestamp; deleted alongside .g2g-goal)
.g2g-goal.lock
# G2G transient lock-mutation mutex (held for milliseconds; debris only after a crash)
.g2g-goal.mutex/

# Claude settings (but not the plugin config, safety hook, and project commands)
.claude/*
!.claude/g2g.json
!.claude/settings.json
!.claude/commands/

# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
out/

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Testing
coverage/

# Misc
*.bak
*.tmp
